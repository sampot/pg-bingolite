# 賓果閃燈（`pg-bingolite`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-bingolite.yaml`

## 1. 一句話

押娛樂幣讓跑燈開號、卡上自動點亮、連線結算獎金的復古賓果閃燈機——純娛樂計分，非任一商業機台復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-bingolite` / `game` / `機台` |
| status | `listed` |
| 模式 | 單人機台；一局＝開 20 號後結算（無關卡） |
| 局成本／起始幣 | 每局 **10** 幣（`ROUND_COST`）；開局 **100** 幣（`START_CREDITS`）；+50 幣按鈕無限補充 |
| 卡面 | 5×5、中心免費格；號碼池 1–75（`POOL_SIZE`）依 B/I/N/G/O 分欄 |
| 獎金倍率 | 1–5 線 ×2/×5/×12/×25/×40；全盤 ×80 |
| 素材 | 全部 DOM/CSS 版面＋WebAudio 合成音效（無圖檔資產） |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx vitest run` 測試（現無測試檔） |

## 3. 完整規則（現行實作）

### 3.1 目標與流程

1. 加幣 → 按「開始一局」扣 10 幣並鎖定操作（`beginRound`：餘額不足或已在跑即拒絕）。
2. 從 1–75 洗牌取前 **20** 個為本局開號序列（`DRAW_COUNT=20`），逐球播放跑燈動畫。
3. 每球若命中卡面格則該格永久亮起（`applyDraw`）；中心免費格（值 0）開局即標記。
4. 20 球開完立即結算（`settle`）：清點橫線／直線／兩對角線完成數與是否全盤，發放娛樂獎金。

### 3.2 卡面生成

- 每欄自對應區間洗牌取 5 個：B=1–15、I=16–30、N=31–45、G=46–60、O=61–75（`COL_RANGES`）；Fisher–Yates 洗牌。
- 中央 `[2][2]` 固定為 0＝FREE，永遠視為已標記。

### 3.3 賠付表（實際值，相對一局 10 幣）

| 完成線數 | 1 | 2 | 3 | 4 | 5 | 全盤（25 格皆亮） |
| --- | --- | --- | --- | --- | --- | --- |
| 倍率（`LINE_MULT`/`BLACKOUT_MULT`） | ×2 | ×5 | ×12 | ×25 | ×40 | ×80 |

- 全盤成立時**取代**線數賠付（`payoutFor` 先判 blackout）；超過 5 線以 5 線倍率計。

### 3.4 跑燈動畫（機台感核心）

- 開號顯示器外圈 20 顆 LED（`CHASE_SLOTS=20`）跑燈追逐：起點隨機、總步數＝圈數×20＋走到目標格距離，圈數 2–3 圈隨機（`buildChasePath`）。
- 步進間隔 `stepDelayMs`：前段加速（t<0.55 由 123ms 收斂到 42ms）、後段戲劇性減速（二次曲線最長約 266ms），模擬機台「快→慢→停」節奏。
- 追逐途中顯示器閃現誘導假號碼 `((slot×7+3) mod 75)+1`；停格後才揭示真號碼。
- 每球之間固定停 380ms 供玩家核對；全部球數動畫期間控制列停用。

### 3.5 邊界處理

- `running` 期間所有變更操作（換卡／清除／加幣）被 `game.js` 擋下回傳 false；UI 同步停用按鈕。
- 換新卡（`newCard`）會重置標記與開號紀錄且**不退款**；「清除本局」只清標記保留同一張卡。
- 中途無中止路徑（僅 `abort()` 內部備用，正常 UI 不觸發）；音效 AudioContext 需首次 pointerdown 解鎖。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| +50 幣 | 加 50 枚娛樂幣 |
| 開始一局 | 扣 10 幣、跑燈開 20 號 |
| 新卡 | 重生 5×5 卡（不退已扣幣） |
| 清除本局 | 清標記與紀錄、留卡 |
| 音效開／關 | 靜音切換（僅記憶體） |

- 版面為 DOM 格線（非 canvas）：上方開號顯示器＋LED 環、左側已開號 chips（命中者高亮）、右側 5×5 卡；狀態列顯示娛樂幣／上局獎金與訊息。
- Mobile-first 直向單欄；禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| （無）KV | — | 本 repo 目前完全未接 `/api/kv`；娛樂幣、戰績皆存頁面記憶體，重載即失 |
| （無）localStorage | — | 未使用 |

- functions.js 為 stub（回 `{ok,name,path}` JSON），無自訂 API。
- 新增任何跨局統計（最高獎金、總局數等）一律走 `/api/kv/pg-bingolite-*`，禁止裸 localStorage 當權威。

## 6. 美術／音效／署名

- 無外部圖像資產：版面、LED、卡片全由 CSS/DOM 繪製；音效為 WebAudio 振盪器合成（`audio.js` master 0.24：投幣／跑燈 tick 頻率隨 urgency 升高／中卡／連線琶音步數隨倍率 4/5/7 變化／未中低鳴），無需 ATTRIBUTION。
- 若未來加入素材：拷進 `assets/`、新增 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` 的 files。

## 7. 測試（`npx vitest run`）

**現況零測試**（repo 無任何 test 檔）。最小必測建議（先寫失敗測試再加功能）：

1. `generateCard`：每欄數字落在對應區間、不重複、中心為 0。
2. `findWinLines`：構造已知 marked 矩陣驗證橫／直／對角線計數。
3. `payoutFor`：0 線 0 分、各線數倍率、blackout 覆蓋線數賠付。
4. `beginRound`：扣 10 幣、回傳 20 個不重複號碼、餘額不足擲錯。
5. `applyDraw`：命中標記並回報座標、未中不影響卡面。
6. `settle`：派彩入帳、`running` 復位。
7. `buildChasePath`：首元素在界內、末元素恆等於 targetIndex。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；訊息一律狀態列／頁內 UI。
3. Mobile-first；主操作不可 hover-only。
4. **純娛樂定位**：娛樂幣與「獎金」皆虛擬名目，不得接入真實金錢／兌現機制；型錄與 README 的純娛樂聲明須保留。
5. 新增跨局進度以 `/api/kv/{key}` 為唯一權威；禁止裸 localStorage 當權威。
6. 不自行載入 `sdk.js`；宿主注入 `window.PG`。本作未用 `PG.libs`（vanilla 即可）。
7. 改動可執行邏輯前先寫失敗測試（TDD）。
8. 檔案清單變動須同步 `sam-manifest.json`。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化機台手感與重玩誘因，不改變「跑燈開號＋連線結算」的核心認同。

**高優先**

1. **戰績持久化**：以 `/api/kv/pg-bingolite-stats` 存最高單局獎金／總局數／連線次數，HUD 顯示生涯數字——目前重載歸零，毫無累積感。
2. **開號快轉**：一局 20 球（ chase＋380ms 停等）偏拖；提供「按住加速」或尾段 3 球才減速的完整儀式感，中段可跳——保留懸念同時尊重時間。
3. **差一球提示**：每次開號後計算「距最近一條連線還差 N 格」，於狀態列顯示（如「再 1 格連線！」），把被動看號變成主動期待。

**中優先**

4. **音效偏好持久化**：mute 目前僅記憶體；比照其他機台寫 localStorage 輕量快取即可（非權威資料）。
5. **留卡 vs 換卡的決策**：現行換卡免費使策略為空；可讓同一張卡連續遊玩累積小加成（如第 3 局起 1 線 ×3），製造取捨。
6. **全盤倒數氛圍**：剩 1–2 格未亮時 LED 環轉特殊色、tick 音調升高——低成本放大終盤張力。

**低優先**

7. 得分彩光粒子沿 win-line 流動、卡片格呼吸動畫。
8. 自動連局模式（結算後 2 秒自動再開，可隨時取消），服務「掛機看燈」的休閒玩法。
